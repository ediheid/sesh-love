const NavBar = () => {
  return (
    <nav className="w-full flex items-center justify-between px-4 py-4 md:py-6">
      <div>logo / contact (conditionally depending on view)</div>
      <div>'menu' for dropdown menu</div>
    </nav>
  );
};

export default NavBar;
